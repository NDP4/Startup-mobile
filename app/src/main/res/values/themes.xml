<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Booking_bus" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
<!--    <style name="Theme.Booking_bus" parent="Theme.MaterialComponents.Light.NoActionBar">-->
<!--        &lt;!&ndash; Other theme attributes &ndash;&gt;-->
<!--    </style>-->

    <!-- Splash Screen theme -->
    <style name="Theme.Booking_bus.Splash" parent="Theme.Booking_bus">
        <item name="android:windowBackground">@color/white</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>

    <style name="Theme.Booking_bus" parent="Base.Theme.Booking_bus">
        <item name="android:windowActivityTransitions">true</item>
        <item name="android:windowEnterTransition">@android:transition/fade</item>
        <item name="android:windowExitTransition">@android:transition/fade</item>
    </style>
</resources>